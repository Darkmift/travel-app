import { Box, Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Box>
        <Typography variant="h1" component={'h1'}>
          Home
        </Typography>
      </Box>
    </Container>
  );
};

export default Home;
